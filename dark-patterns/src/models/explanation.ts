class Explanation {
  title: string;
  explanation: string;
  goal: string;

  constructor(title: string, explanation: string, goal: string) {
    this.title = title;
    this.explanation = explanation;
    this.goal = goal;
  }
}

export default Explanation;
